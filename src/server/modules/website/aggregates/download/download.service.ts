import { RequestParams } from '@app/core';
import { DownloadFilesService } from '@app/website';
const downloadFilesService: DownloadFilesService = {
  create: async (_data: any, _params: RequestParams<any>): Promise<any> => {
    //
  },
};

export default downloadFilesService;
